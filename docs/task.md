# Plano de Trabalho - Resolução de `FirebaseError: [code=permission-denied]`

- [ ] Verificar implantação das regras Firestore (`firebase deploy --only firestore:rules`).
- [ ] Confirmar existência do documento `user_profiles/{uid}` no console do Firestore.
- [ ] Testar a aplicação em modo incógnito para garantir que o erro foi resolvido.
