# ==================================================
# Web Quiz Medicina - Environment Variables
# ==================================================
# Copy this file to .env and fill with your actual values
# NEVER commit .env to version control!

# --------------------------------------------------
# Supabase Configuration
# --------------------------------------------------
# Projeto: WebQuizMedicina (ID: bwgglfforazywrjhbxsa)
# Região: sa-east-1 (São Paulo, Brasil)
# Status: ACTIVE_HEALTHY
VITE_SUPABASE_URL=https://bwgglfforazywrjhbxsa.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJ3Z2dsZmZvcmF6eXdyamhieHNhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjMzMDIwMzMsImV4cCI6MjA3ODg3ODAzM30.Ngf582OBWuPXO9sshKBYcWxk8J7z3AqJ8gGjdsCyCkU

# --------------------------------------------------
# Security Configuration (Edge Functions Only)
# --------------------------------------------------
# These variables are used in Edge Functions (Deno runtime)
# Set them via Supabase Secrets in production:
#   supabase secrets set GEMINI_API_KEY=your_actual_api_key_here --project-ref bwgglfforazywrjhbxsa
#   supabase secrets set ALLOWED_ORIGIN=https://web-quiz-medicina.vercel.app --project-ref bwgglfforazywrjhbxsa
#   supabase secrets set ENVIRONMENT=production --project-ref bwgglfforazywrjhbxsa

# Gemini AI API Key
# Get from: https://makersuite.google.com/app/apikey
# Used by Edge Functions for AI generation
# ⚠️ NEVER commit your actual API key! Use Supabase Secrets for production
GEMINI_API_KEY=your_gemini_api_key_here

# Allowed CORS Origins (Production)
# Default: '*' (allows all - only for development!)
# IMPORTANT: Set to your actual domain in production
# Examples:
#   ALLOWED_ORIGIN=https://web-quiz-medicina.vercel.app
#   ALLOWED_ORIGIN=https://yourdomain.com
ALLOWED_ORIGIN=*

# Environment
# Options: development | production
# Controls security features like:
#   - HSTS headers (production only)
#   - Error verbosity (detailed in dev, minimal in prod)
#   - Console logging (enabled in dev)
ENVIRONMENT=development

# --------------------------------------------------
# Optional Security Configuration
# --------------------------------------------------

# HMAC Secret for Request Signing (optional)
# Generate with: openssl rand -hex 32
# Used for webhook verification or API-to-API auth
# HMAC_SECRET=your_random_secret_here

# Rate Limiting (optional - override defaults)
# AI_GENERATION_RATE_LIMIT=10
# CHAT_RATE_LIMIT=30
# READ_RATE_LIMIT=100

# --------------------------------------------------
# Notes
# --------------------------------------------------
# 1. Frontend (.env):
#    - Only VITE_* variables are exposed to browser
#    - Keep sensitive keys out of VITE_* vars
#
# 2. Edge Functions (Supabase Secrets):
#    - GEMINI_API_KEY, ALLOWED_ORIGIN, etc.
#    - Set via: supabase secrets set KEY=value --project-ref bwgglfforazywrjhbxsa
#    - Access via: Deno.env.get('KEY')
#
# 3. Security Best Practices:
#    - Never hardcode secrets in code
#    - Use .env for local development only
#    - Use Supabase Secrets for production
#    - Rotate secrets regularly
#    - Use different keys for dev/staging/prod
#
# 4. Important: Rotate the exposed API key!
#    - The previous key in this file was committed to Git
#    - Generate a new key at: https://makersuite.google.com/app/apikey
#    - Delete the old key from Google AI Studio
#    - Set the new key using Supabase Secrets (not in this file!)
